<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <!-- Server Action for Confirm the Booking -->
	    <record id="action_confirm_booking" model="ir.actions.server">
            <field name="name">Confirm Booking</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_restaurant_booking"/>
            <field name="binding_model_id" ref="model_restaurant_booking"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">records.button_confirm()</field>
        </record>
</odoo>