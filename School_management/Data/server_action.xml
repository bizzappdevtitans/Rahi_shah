<?xml version="1.0" encoding="utf-8"?>
<odoo>

	    <record id="action_confirm_submission" model="ir.actions.server">
            <field name="name">Confirm Submission</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_school_submit"/>
            <field name="binding_model_id" ref="model_school_submit"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">records.button_confirm()</field>
        </record>


	    <record id="action_confirm_admission" model="ir.actions.server">
            <field name="name">Confirm Admission</field>
            <field name="type">ir.actions.server</field>
            <field name="model_id" ref="model_school_admission"/>
            <field name="binding_model_id" ref="model_school_admission"/>
            <field name="binding_view_types">list,form</field>
            <field name="state">code</field>
            <field name="code">records.button_confirm()</field>
        </record>
</odoo>